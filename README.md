run project:
npm install
npm run dev

project files explenation:
page layout => shows topbar, sidebar, and children (requestsPage)
requestspage => top buttons, and requestsTable
requestsTable=> shows summary bar, table, and table footer
